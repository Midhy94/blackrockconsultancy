# Used at build time for base URL hints (sitemap / metadata if you add them).
NEXT_PUBLIC_SITE_URL=https://www.blackrocksconsultancy.com

# Optional: site built for a subpath (e.g. example.com/demo)
# NEXT_PUBLIC_BASE_PATH=demo

# --- Forms use PHP + SMTP on the server (see public/forms/config.example.php) ---
# No SMTP variables here for production static hosting.
