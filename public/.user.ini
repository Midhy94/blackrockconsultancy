; Applied by many hosts when this file sits in the web root (uploaded as out/.user.ini).
; If cPanel ignores it, set the same values in MultiPHP INI Editor.
upload_max_filesize = 12M
post_max_size = 12M
